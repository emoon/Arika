#include <Arika/Arika.h>
#include <assert.h>

extern ARFuncs* g_arFuncs;

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

struct ARWidget* ar_window_create_main()
{
	assert(g_arFuncs);
	return g_arFuncs->window_create_main();
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

struct ARWidget* ar_window_create()
{
	assert(g_arFuncs);
	return g_arFuncs->window_create();
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

struct ARWidget* ar_button_create()
{
	assert(g_arFuncs);
	return g_arFuncs->button_create();
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

struct ARWidget* ar_listview_create()
{
	assert(g_arFuncs);
	return g_arFuncs->list_view_create();
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

struct ARLayout* ar_layout_vbox_create()
{
	assert(g_arFuncs);
	return g_arFuncs->layout_vbox_create();
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

struct ARLayout* ar_layout_hbox_create()
{
	assert(g_arFuncs);
	return g_arFuncs->layout_hbox_create();
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

int ar_layout_add(struct ARLayout* layout, struct ARWidget* widget)
{
	return g_arFuncs->layout_add(layout, widget);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

int ar_layout_remove(struct ARLayout* layout, struct ARWidget* widget)
{
	return g_arFuncs->layout_remove(layout, widget);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

int ar_widget_set_title(struct ARWidget* widget, const char* title)
{
	return g_arFuncs->widget_set_title(widget, title);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

int widget_set_text(struct ARWidget* widget, const char* text)
{
	return g_arFuncs->widget_set_text(widget, text);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

int ar_widget_set_width(struct ARWidget* widget, int width)
{
	return g_arFuncs->widget_set_width(widget, width);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

int ar_widget_set_height(struct ARWidget* widget, int height)
{
	return g_arFuncs->widget_set_height(widget, height);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

int ar_widget_set_layout(struct ARWidget* widget, struct ARLayout* layout)
{
	return g_arFuncs->widget_set_layout(widget, layout);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

int ar_widget_attach(struct ARWidget* parent, struct ARWidget* widget)
{
	return g_arFuncs->widget_attach(parent, widget);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

int ar_update()
{
	return g_arFuncs->update();
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

int ar_ui_load(const char* filename)
{
	return g_arFuncs->ui_load(filename);
}

